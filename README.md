# Add Serial Number to Markdown file

```angular2html
python add.py -i [path of original markdown file] 
              -o [path of target markdown file(with serial number) 
              -m [0: end with blank space; 1: ent with .]]
```